enum HTTPConstant {
    enum QueryParameter: String {
        case latitude = "lat"
        case longitude = "lon"
        case apiKey = "appid"
        case units = "units"
    }
    
    enum DictionaryValue: String {
        case metric = "metric"
    }
}
